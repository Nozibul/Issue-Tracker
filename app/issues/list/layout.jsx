
export const metadata = {
    title: "Issue list",
    description: "Issue Tracker List of Item",
  };

const IssueLayout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default IssueLayout  ;